import SideBar from "../components/SideBar";

export default function UserPage(props) {
  return (
    <section class="columns">
      <SideBar />
      {/* flashcards go here */}
    </section>
  );
}
